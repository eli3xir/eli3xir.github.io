# 网站整体升级改造计划

> 记录于 2026-07-30，分支 `redesign`。

## 核心意图

这个分支承载我对个人网站的真实想法：**对整个网页做一次彻底的升级改造，完全脱离已有的实现框架**，而不是在现有代码上修修补补。

- 现有站点（`main` 分支上的一切：纯静态无框架结构、四季/节日主题引擎、皮肤中心、转场系统、全局播放器、实验室等）作为参考和素材来源保留，但**不作为新版本的基底**。
- 新版本的技术选型、信息架构、视觉体系都重新决定，不受现状约束。
- 具体的设计想法暂未展开，后续逐步补充到本文件。

## 建模工作流（已验证）

首个试探案例：壁灯建模。

- **管线**：Three.js 程序化建模 → Playwright 截图迭代外观 → 浏览器内 GLTFExporter 导出 `.glb` → 回读验证
- **工具**：已拆分为独立仓库 **model-studio**（MIT 协议，本地 `D:\model-studio`），本仓库只消费导出的资产
- **产出**：`assets/models/wall-lamp.glb`（725KB，含透射玻璃、实体气泡、黄铜 PBR 材质、点光源）
- **待改进**：灯管内气泡可见度、发光效果（在 model-studio 仓库迭代后重新导出替换）
- **结论**：全流程无需安装专业软件；后续更精细的模型可引入 Blender 无界面脚本（`blender --background --python`）
- **方向**：网站各板块将逐步做建模化改造，3D 资产统一放 `assets/models/`

## 核心愿景：3D 房间式首页（2026-07-30 记录）

首页完全 3D 建模化——**访客进入一个房间**，鼠标移动带动视角（第一人称/过肩视角环视）。
所有站点功能都是房间里的实体物件，点击物件进入对应板块：

| 板块 | 房间里的物件 | 交互 |
| --- | --- | --- |
| 电台 | 留声机 | 真的在播放，可以切换碟片 |
| 实验室 | 桌子上的实验器皿 | 每个器皿里是不同的药水/效果 |
| 文章 | 笔记本 | 翻开阅读 |
| 关于 | 墙上的便签 | 凑近查看 |
| 皮肤 | 墙纸 | 换皮肤 = 换房间墙纸/装潢 |
| 壁灯 | 墙上的壁灯（已建模：wall-lamp.glb） | 房间照明/开关 |

整体质感对标 3D 游戏画面。关键词：场景化导航、拟物化、沉浸式。

## 美术与镜头语言（2026-08-03 记录）

- **美术风格**：写实 PBR + 完全真实的光照（不做低多边形卡通）
- **镜头**：默认**锁视角的 2.5D**——横向跟随鼠标，整体以平移为主、只有微微旋转（最左到最右一个小角度），类似横版场景/娃娃屋侧视图
- **导航**：点击房间里的物件 → 相机运镜跳转（dolly/推轨变焦到该物件的特写机位）→ 进入对应板块；返回则运镜拉回全景
- 参考感受：横版房间剖面（类似 Fallout Shelter / This War of Mine 的侧视房间），但画面写实

### 技术推论

- 真实光照走**烘焙**路线：房间整体在 Blender（或 Three.js 手工）搭好后烘焙 lightmap/AO，实时只留少量动态光——纯实时光追级 GI 在浏览器里跑不动
- 相机器件：窄 FOV 透视相机 + 水平平移范围钳制 + 微旋转 + 惯性缓动；物件 hover 高亮（描边/发光）；点击用 GSAP 做相机动画到各模块锚点
- 壁灯等已有 GLB 直接摆进场景

## 待补充

- [ ] 新版的技术栈选型
- [ ] 信息架构 / 页面结构
- [ ] 视觉与交互方向
- [ ] 现有内容（文章、实验、皮肤等）的取舍与迁移策略
